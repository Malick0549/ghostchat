{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "PYTHONPATH=/app gunicorn --worker-class eventlet --workers 1 --bind 0.0.0.0:8080 backend.flask_app:app",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}