# GhostChat backend dependencies
# Install with: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
flask-session>=0.6.0

# ── Database + ORM ────────────────────────────────────────────────
flask-sqlalchemy>=3.1.0
flask-bcrypt>=1.0.1

# ── Cryptography ──────────────────────────────────────────────────
cryptography>=42.0.0

# ── Image validation (verify uploaded avatars/media are real images,
#    not just extension-matched files) ──────────────────────────────
Pillow>=10.0.0

# ── Rate limiting (brute-force / abuse protection) ────────────────
flask-limiter>=3.5.0

# ── WebSocket / Realtime ─────────────────────────────────────────
# FIX: python-engineio was previously unpinned, so pip installed whatever the
# latest release was — which changed its internal disconnect-handler calling
# convention (adds a "reason" argument) in a way python-socketio==5.10.0 did
# not expect, causing "_handle_eio_disconnect() takes 2 positional arguments
# but 3 were given" on every disconnect. This trio is confirmed mutually
# compatible via PyPI's own dependency metadata (flask-socketio 5.6.1
# requires python-socketio>=5.12.0, which requires python-engineio>=4.13.2).
flask-socketio==5.6.1
eventlet==0.33.3
python-socketio==5.16.3
python-engineio==4.13.3

# ── Production Server ────────────────────────────────────────────
gunicorn==21.2.0

# ── Dev / testing (optional) ─────────────────────────────────────
pytest>=8.0.0
pytest-flask>=1.3.0
